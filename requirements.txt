# ── AI Employee OS (조직 엔진 + 웹) ──
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9

# ── Instagram 실행 파이프라인 (src/, 추후 연결) ──
anthropic>=0.40.0
requests>=2.31.0
PyYAML>=6.0
python-dotenv>=1.0.0
Pillow>=10.0.0

# (선택) OpenAI 사용 시
openai>=1.40.0
